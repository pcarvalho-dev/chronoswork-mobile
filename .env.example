# API Configuration for Development
# Copy this file to .env and update with your settings

# Force API URL (useful for physical devices when auto-detection fails)
# Set to true to use EXPO_PUBLIC_API_URL instead of auto-detection
EXPO_PUBLIC_FORCE_LOCAL_IP=true

# Your computer's local IP address and port
# Find your IP: On Linux run: hostname -I | awk '{print $1}'
# On macOS: ipconfig getifaddr en0
# On Windows: ipconfig (look for IPv4 Address)
EXPO_PUBLIC_API_URL=http://192.168.1.226:8000

# Usage:
# - For physical devices: Set FORCE_LOCAL_IP=true and update the IP
# - For emulator: Set FORCE_LOCAL_IP=false or remove .env file
# - The app will use auto-detection if .env is not present
